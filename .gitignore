# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env / secrets
.env
.env.*
!.env.example

# Build / cache
*.tsbuildinfo
.vite
coverage

# Scratch research artifacts + browser-automation output (orthogonal-lane spike)
.research
.playwright-mcp

# Spine-build orchestration scripts + milestone screenshot evidence (tooling, not app source)
build-workflows
m0-*.png
m1-*.png
m2-*.png
m3-*.png

# R1 spike: the results/*.json measurements ARE committed (they are the empirical
# evidence cited by docs/adr/0008). Only the transient server log is ignored —
# covered by the *.log rule above (spikes/r1-music21/results/uvicorn.log).

# alphaTab assets auto-copied from node_modules by @coderline/alphatab-vite (derived)
public/font
public/soundfont

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
